Theme-specific endpoints registered by the **wpdev** theme.

These routes expose headless CMS functionality: custom post types, site configuration,
and any other business logic built into this theme. All routes require authentication
via an API key (`X-API-Key`) unless marked public.
